# Logic of my zsh:


```
bindkey stuff        -> keybindings.zsh
compinit + zstyle    -> completion.zsh
dircolors            -> completion.zsh or exports.zsh
starship             -> prompt.zsh
history              -> history.zsh
aliases              -> aliases.zsh
helpers              -> functions.zsh
PATH/login setup     -> .zprofile
```
