#!/usr/bin/env bash


# Makes promtp pretty (my old way)
# source ${HOME}/.prompt

# Homebrew configuration:
if [ -d /home/linuxbrew/ ]; then
    eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
    echo "Brew Package Manager Activated"
else
    echo "No brew installation"
fi